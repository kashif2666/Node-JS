/*
fs=> this fs module is a core module that allows us to interact with file system.
It is used to create, read, update and delete file and folder.

*/

const fs= require('fs');
const path= require('path');
